library identifier: 'JenkinsJenkins_Pipeline_Shared_Libs@main',
        retriever: modernSCM([$class: 'GitSCMSource', remote: 'https://github.com/rocky019/Dotnet_Project.git'])

pipeline {
    agent any
    stages {
        stage('Audit tools') {
            steps {
                auditTools()
            }
        }
    }
}
